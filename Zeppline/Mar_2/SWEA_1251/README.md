프림 알고리즘을 수행하기 위한 Node 클래스 선언

map, graph, 변수 선언 및 입력값 주입

graph 배열에 연산 수식을 적용한 가중치 주입

우선순위 큐 선언 및 초기값 add

큐의 Node가 소진될때까지 while문 반복
> Node의 값을 로컬 변수에 주입
>
> 현재 좌표을 이미 탐색한 경우 continue
>
> 결과 값에 현재 가중치 가산
>
> 다음 좌표에 방문 가능할 경우, 좌표와 해당 좌표의 가중치를 담은 Node를 큐에 add

소수점 반올림한 결과 출력